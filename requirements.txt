# Core pipeline
numpy>=1.24
scipy>=1.10
scikit-image>=0.21
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.0
torchvision>=0.15
torch-geometric>=2.4

# Optional. statsmodels supplies McNemar's exact test (scipy binomial fallback
# exists); imageio backs the JPEG perturbation in the robustness study.
statsmodels>=0.14
imageio>=2.31

# NOTE: torch-scatter and torch-sparse are deliberately NOT listed. PyG 2.x
# does not need them for anything this pipeline uses, and pip builds them from
# source, which on Kaggle either takes ~20 minutes or fails outright.
